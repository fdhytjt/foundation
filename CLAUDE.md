# foundation · 知识库底座（学习项目）

从零实现一个「带权限、可检索、可接入外部数据」的知识库底座，用来系统学习后端工程。

## 当前进度

- **阶段**：0 环境准备（进行中）
- **当前计划**：`docs/plan/00-环境准备.md`
- **下一步**：安装 JDK 17 和 Maven，启动 Docker

## 文档地图

| 位置 | 内容 |
|---|---|
| `README.md` | 项目介绍、8 阶段路线图总览 |
| `docs/plan/` | 每个阶段的详细计划：目标、学习要点、任务、验收标准、复盘 |
| `docs/decisions/` | 设计决策记录：背景、选项对比、决定和理由 |
| `docs/notes/` | 学习笔记：概念、我的理解、踩坑、自测题 |

## 协作方式（最高优先级）

- 我是学习者。**先讲原理，再写代码**；新概念讲完出 2-3 道题检查理解。
- 阶段 1-3 核心代码由我自己写，你只给提示和指出问题，我明确要求时才给完整代码。
- 阶段 5 以后，样板代码可以由你写，核心逻辑（权限、检索、同步）仍由我写。
- 设计选择要给出**理由和备选方案**，由我拍板，然后记入 `docs/decisions/`。
- 一个功能一个提交。

## 文档维护规则

1. **开始一个阶段前**，先按 `docs/plan/_模板.md` 写该阶段计划。**只提前写一个阶段。**
2. **做出重要设计选择时**，按 `docs/decisions/_模板.md` 新增一篇，编号递增，并更新索引。
3. **阶段结束时**：填写计划的复盘，写对应的学习笔记，更新 README 路线图状态和本文件的「当前进度」。
4. 本文件只放稳定的规则，控制在 100 行以内；细节写进 `docs/`。

## 架构原则

1. **分层，依赖只朝内**：web → service → 数据访问 → domain，公共模块横切。
2. **权限唯一闸**：人和程序走同一套鉴权；数据权限**下推 SQL**，禁止查出来再过滤。
3. **默认拒绝**：算不出权限时返回空结果。
4. **外部皆适配器**：外部数据源实现 `Connector + Normalizer`，核心代码不依赖具体外部系统。
5. **检索维度独立成列并建索引**，不埋在 JSON 里。

## 约定

- **技术栈**：Java 17 · Spring Boot 3 · Maven · MySQL 8（Docker 本地运行） · JUnit 5；ORM 等选型在对应阶段决策。
- **包名**：`io.github.fdhytjt.foundation`
- **提交信息**：`类型: 中文描述`，类型用 `feat` / `fix` / `docs` / `test` / `refactor` / `chore`。
- **密钥**：密码、Token、密钥只走环境变量，禁止写进代码、配置文件或提交记录。
- **测试**：每个接口必须有测试；测试全绿才能提交。
- 编码规范（返回格式、异常、数据库命名等）在阶段 1、2 决策后补充到这里。
