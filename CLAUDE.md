# Agent 平台学习项目

精读一个生产级 Agent 平台的源码，再用简化代码仿写它的核心机制，系统学习后端与 AI 工程。

## 保密规则（最高优先级）

本仓库在 GitHub **公开**，参考项目是**不公开的公司项目**。

- 参考项目的路径、名称、源码、类名、表名、接口路径、业务细节，以及公司和内部系统名称，**只能写进 `private/`**（已被 `.gitignore` 忽略）。
- 公开文件（本文件、`README.md`、`docs/`、代码）只写**通用概念**和**自己的仿写代码**，禁止复制参考项目的源码。
- 每次提交前检查暂存区，确认没有 `private/` 下的文件，也没有上述敏感信息。

**参考项目的位置、模块对照和精读路线见 `private/README.md`**（本地文件，开始学习任务前先读）。

## 当前进度

- **阶段**：0 环境准备（进行中）
- **当前计划**：`docs/plan/00-环境准备.md`
- **下一步**：IDEA 中设置 Maven home 并打开 `~/tutu`；写学习笔记 `docs/notes/00-开发环境.md`（JDK、Maven、Docker、IDEA 已安装）

## 文档地图

| 位置 | 内容 | 是否公开 |
|---|---|---|
| `README.md` | 项目介绍、学习路线图 | 公开 |
| `docs/plan/` | 每个阶段的计划：目标、学习要点、任务、验收标准、复盘 | 公开 |
| `docs/decisions/` | 仿写时的设计决策记录 | 公开 |
| `docs/notes/` | 通用概念笔记 | 公开 |
| `private/` | 参考项目信息、源码精读笔记 | **仅本地** |

## 每个阶段的学习循环

```text
① 精读：先讲原理，再带读参考项目对应模块的源码 → 精读笔记写入 private/notes/
② 仿写：在本仓库用简化代码实现同一机制 → 设计选择写入 docs/decisions/
③ 对照：比较仿写版和参考项目的差异，理解对方为什么做得更复杂
④ 复盘：填写阶段计划的复盘，把通用概念写入 docs/notes/
```

## 协作方式

- 我是学习者。**先讲原理，再看代码**；新概念讲完出 2-3 道题检查理解。
- 仿写的核心代码由我自己写，你给提示、指出问题；我明确要求时才给完整代码。样板代码可以由你写。
- 设计选择要给出**理由和备选方案**，由我拍板。
- **参考项目只读**，不修改、不提交、不启动会连接远程数据库的服务。
- 一个功能一个提交。

## 文档维护规则

1. **开始一个阶段前**，按 `docs/plan/_模板.md` 写该阶段计划。**只提前写一个阶段。**
2. **做出重要设计选择时**，按 `docs/decisions/_模板.md` 新增一篇，编号递增，并更新索引。
3. **阶段结束时**：填写复盘，写笔记，更新 README 路线图状态和本文件的「当前进度」。
4. 本文件只放稳定的规则，控制在 100 行以内；细节写进 `docs/` 或 `private/`。

## 约定

- **技术栈**：Java 17 · Maven 多模块 · JUnit 5；Spring Boot、MySQL、React 在用到的阶段引入，选型写入决策记录。
- **包名**：`io.github.fdhytjt.foundation`
- **提交信息**：`类型: 中文描述`，类型用 `feat` / `fix` / `docs` / `test` / `refactor` / `chore`。
- **密钥**：API Key、密码、Token 只走环境变量，禁止写进代码、配置文件、日志或提交记录。
- **测试**：仿写代码必须有测试；测试全绿才能提交。
