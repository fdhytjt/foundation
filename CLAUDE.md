# tutu · 知识库底座（学习项目）

从零实现一个「带权限的知识库底座」：组织架构 → 账号权限 → 知识存储 → 权限检索 → 数据采集。
参考原型：一个已在生产使用的组织知识库后端（只作设计对照，不复制代码）。路线图与进度见 `README.md`。

## 协作方式（最高优先级）

- 我是学习者。**先讲原理，再写代码**；新概念讲完出 2-3 道题检查理解。
- 阶段 1-3 核心代码由我自己写，你只给提示、指出问题，不直接给完整答案；我明确要求时再给代码。
- 阶段 5 以后，样板代码（DO / Mapper / 简单 CRUD）可以由你写，核心逻辑（权限、检索、同步）仍由我写。
- 每个设计选择说明**理由 + 备选方案**，由我拍板。
- 一个功能一个提交；做完一个阶段更新 `README.md` 进度和本文件的规范。

## 技术栈

Java 17 · Spring Boot 3 · Maven 多模块 · MyBatis-Plus · MySQL 8（Docker 本地运行） · JUnit 5 + Mockito

## 架构原则

1. **分层，依赖只朝内**：`web → service → mapper → domain`，`common` 横切。
2. **权限唯一闸**：人和程序走同一套鉴权；数据权限**下推 SQL**，禁止查出来再过滤。
3. **默认拒绝**：算不出权限时返回空结果，而不是返回全部。
4. **外部皆适配器**：外部数据源一律实现 `Connector + Normalizer`，核心代码不依赖具体外部系统。
5. **检索维度独立成列并建索引**，不埋在 JSON 里。

## 编码规范

- Controller 统一返回 `CommonResult`；业务错误抛 `CommonException`，交给全局异常处理。
- 逻辑删除用 `status` 字段，不物理删除。
- 多表写操作加 `@Transactional(rollbackFor = Exception.class)`。
- 密码、Token、密钥只走环境变量，禁止写进代码、配置文件或提交记录。
- 每个接口必须有测试；`mvn test` 全绿才能提交。

## 常用命令

```bash
mvn clean package -DskipTests   # 编译打包
mvn test                        # 运行测试
```
